# Copiar a .env y ajustar valores (no subir .env a Git)

POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass
POSTGRES_DB=auth_course
POSTGRES_PORT=5432

APP_PORT=8000

# Clave para firmar JWT (generar una cadena larga y aleatoria en producción)
SECRET_KEY=cambiar-en-produccion
JWT_ALGORITHM=HS256
